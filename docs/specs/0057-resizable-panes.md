# 0057: 3つの領域の幅をドラッグで変えられるようにする

- 対応issue: [0057](../issues/0057-resizable-panes.md)
- 状態: draft
- 作成日: 2026-09-20

## 目的

パレット180px・ソース630px・プレビュー630px（幅1440px）はCSSが決めていて、
利用者は変えられない。書いているときはソースを、読み返すときはプレビューを
広くしたい。領域の境目をドラッグして分け方を変えられるようにし、
その分け方を次に開いたときも保つ。

## 実測（下限を決めるために先に測った）

2026-09-20、幅1440×900・日本語。`grid-template-columns` を変えて測った。

### パレットの幅

| 幅 | タブの高さ | `基本` タブのボタン | 検索欄 |
|---|---|---|---|
| 120px | **48px**（`αギリシャ小文字` が2行） | 6行×1列 | 95px |
| 140px | **48px**（同上） | 4行×2列 | 115px |
| **150px** | **30px** | 4行×2列 | 125px |
| 180px（現在） | 30px | 3行×3列 | 155px |
| 240px | 30px | 2行×4列 | 215px |

**150pxがタブ名の折り返さない下限**（`Ωギリシャ大文字` が1行に収まる）。

### ソースとプレビューの分け方（パレット180px、残り1260px）

| ソース | プレビュー | グラフ | ソースの見出し行 | 数式のはみ出し |
|---|---|---|---|---|
| 240px | 1020px | 480px | **64px**（3行に折り返す） | 0件 |
| 300px | 960px | 480px | **47px**（2行） | 0件 |
| **360px** | 900px | 480px | **30px** | 0件 |
| 630px | 630px | 480px | 30px | 0件 |
| 900px | **360px** | **312px** | 30px | 0件 |
| 960px | 300px | 252px | 30px | 0件 |
| 1020px | **240px** | 192px | 30px | **1件** |

- **ソースの下限は360px**。300px以下だと見出し行のボタン3つ
  （`検索` `ファイルに保存` `ファイルを開く`）が折り返し、行の高さが30px → 47pxになる。
- **プレビューの下限は360px**。ここでグラフは312pxで、
  [0037](0037-graph.md)が幅360pxのペインで確かめた値（312×208px）と同じ。
  240pxまで狭めるとブロック数式が1件はみ出す。

## 仕様

### 1. 仕切りは2本、幅1200px以上でだけ動かせる

```
[ パレット ]│[ ソース ]│[ プレビュー ]
           ↑         ↑
        仕切り1     仕切り2
```

- 仕切りは**グリッドの列として6px**を占める（2本で12px）。
- **幅1199px以下では仕切りを出さない。** 横帯（パレットが上）では
  パレットとの境目が縦ではないうえ、幅720px以下は上下分割になるため。
  幅1200px未満に狭めたときは、保存した値を使わず既定の分け方で描く。

### 2. 下限

| 領域 | 下限 | 根拠 |
|---|---|---|
| パレット | **150px** | タブ名が折り返さない下限（実測） |
| ソース | **360px** | 見出し行のボタン3つが1行に収まる下限（実測） |
| プレビュー | **360px** | グラフが312pxで0037の検証値と同じ。240pxで数式がはみ出す |

下限に達したらそれ以上動かない（ドラッグしても値が変わらない）。
**画面が狭くて3つの下限＋仕切り12pxの合計（882px）に足りないときは、
保存値を捨てて既定に戻す**（幅1200px以上でだけ可変なので、通常は起こらない）。

### 3. 操作

- **ドラッグ**: 仕切りを押して左右に動かす。離した時点の値を保存する。
- **キーボード**: 仕切りは `tabindex="0"` で `role="separator"`
  （`aria-orientation="vertical"`）。左右キーで**16px**ずつ動く。
- **ダブルクリック**: その仕切りを既定（パレット180px / ソースとプレビューは半々）に戻す。
- ドラッグ中はカーソルを `col-resize` にする。

### 4. 保存の形

テーマ（`matheditor:theme:v1`）と同じ形で、専用のキーに持つ。

```json
{ "version": 1, "palette": 180, "sourceRatio": 0.5 }
```

- `palette` は**px**（下限150px）。
- `sourceRatio` は**ソースとプレビューの合計に対するソースの割合**（0〜1）。
  pxで持つと、ウィンドウを広げたときにソースだけが広がる。
  **割合で持てば、広げたときに両方が広がる。**
- 読めない・壊れている・版違いなら既定（`palette: 180`, `sourceRatio: 0.5`）。
  保存の失敗は握りつぶす（テーマ・言語と同じ。失われるのは分け方だけ）。
- 変換は `src/lib/paneSizes.ts` の純粋関数にまとめ、単体テストで固定する
  （`loadPaneSizes` / `savePaneSizes` / `clampPaneSizes`）。

**入力と出力の例**（`clampPaneSizes(保存値, 画面の幅)`）:

| 保存値 | 画面の幅 | 結果 | なぜ |
|---|---|---|---|
| `{palette: 180, sourceRatio: 0.5}` | 1440px | そのまま | 下限を満たす |
| `{palette: 100, sourceRatio: 0.5}` | 1440px | `palette: 150` | パレットの下限 |
| `{palette: 180, sourceRatio: 0.95}` | 1440px | `sourceRatio: 0.713` | プレビュー360pxの下限 |
| `{palette: 900, sourceRatio: 0.5}` | 1440px | `palette: 516` | ソース360px＋プレビュー360px＋仕切り12pxを残す |

### 5. 状態の置き場所

`App.tsx` に `paneSizes` の state を持ち、`.app` と `.panes` の
`grid-template-columns` をインラインスタイルで当てる。

[architecture.md](../architecture.md) は「状態は `source` 1つに集約。
例外はUIの一時的な状態だけ」と書いているが、**分け方は保存する状態**なので、
テーマ・言語と同じ並び（保存する設定）に位置づける。
architecture.md の該当箇所に1行足す。

### 対象ファイル

- `src/lib/paneSizes.ts`（新規）— 既定値・下限・読み書き・`clampPaneSizes`。
- `src/lib/paneSizes.test.ts`（新規）— 上の例を含む単体テスト。
- `src/components/PaneDivider.tsx`（新規）— 仕切り1本。ドラッグとキーボード。
- `src/App.tsx` — state と `grid-template-columns` の適用。
- `src/index.css` — 仕切りの見た目、幅1199px以下では出さない。
- `scripts/verify-ui.mjs` — `panes` 区分（新規）。
- `docs/functional-spec.md` / `docs/architecture.md` — 分け方と状態の位置づけ。

## 未確認の前提

- **ドラッグ中の再描画コスト。** `grid-template-columns` を毎フレーム変えると、
  プレビュー（KaTeXで描画済みのDOM）の再レイアウトが走る。
  0007の400節の文書で、ドラッグが引っかからないかを実装時に測る
  （引っかかるなら、離すまで値を反映しない形に変える）。
- `role="separator"` に `tabindex` を付けたとき、Chromiumが
  `getByRole('separator')` で拾えること。
- ダブルクリックとドラッグ開始が競合しないこと。

## 受け入れ基準

### 単体テスト（`npm test`）

- [ ] 既定は `palette: 180` / `sourceRatio: 0.5`
- [ ] 壊れたJSON・版違い・数値でない値は既定に落ちる
- [ ] `clampPaneSizes` が上の表の4例どおりの結果を返す
- [ ] 画面が下限の合計（882px）に足りないときは既定を返す

### ヘッドレスChromium（`node scripts/verify-ui.mjs panes`）

- [ ] 幅1440pxで仕切りが2本あり、どちらも幅6px
- [ ] 仕切り1を右へ100pxドラッグすると、パレットが180px → 280pxになる
- [ ] 仕切り2を左へ100pxドラッグすると、ソースが100px狭く、プレビューが100px広くなる
- [ ] 変えた幅がリロード後も残る
- [ ] パレットを左へ大きくドラッグしても150pxより狭くならない
- [ ] プレビューを右へ大きくドラッグしても360pxより狭くならない
- [ ] ソースを右へ大きくドラッグしても360pxより狭くならない
- [ ] 仕切りをダブルクリックすると既定（180px / 半々）に戻る
- [ ] 仕切りにフォーカスして左右キーを押すと16pxずつ動く
- [ ] 幅1199pxでは仕切りが出ない（横帯）
- [ ] 幅360pxでも仕切りが出ず、上下分割のまま
- [ ] 分け方を変えた状態で幅1199pxへ狭め、1440pxへ戻すと保存した分け方に戻る
- [ ] プレビューを下限（360px）にしたとき、グラフが312px以上で数式がはみ出さない
- [ ] ソースを下限（360px）にしたとき、見出し行が30pxのまま（ボタンが折り返さない）
- [ ] 400節の文書（0007）を開いた状態で仕切りをドラッグしても、
      1回のドラッグの反映が**100ms以内**（実装時に実測して基準の数値を確定する）

### その他

- [ ] `npm test` `npm run build` `npm run lint` が通る
- [ ] 全区分（`node scripts/verify-ui.mjs`）が通る
- [ ] READMEのスクリーンショット（幅1440pxの2枚）を撮り直した

## 検討したが採らなかった案

- **pxで両方を保存する。** ウィンドウを広げたときにソースだけが広がる。
  割合で持てば両方が広がる。
- **仕切りを境界線の上に重ねる（絶対配置）。** 列を消費せずに済むが、
  位置の計算を自分で持つことになる。グリッドの列にすれば
  ブラウザが位置を決める（代償は12px）。
- **幅721〜1199pxでもソースとプレビューだけ可変にする。** 幅721pxでは
  左右が360.5pxずつで既に下限なので、動かせる余地がほとんどない。
  [0054](0054-palette-placement.md)と同じ1200pxの線で揃える。
- **領域を閉じられるようにする。** [0022](../issues/0022-narrow-pane-switch.md)
  （狭い画面での切り替え）と混ざる。別issueのまま残す。

## スコープ外

- 上下分割（幅720px以下）の高さの可変。
- 領域の並べ替え。
- [0056](../issues/0056-palette-header.md)（パレットの見出し）。
  **0056を先に入れる**ので、本issueは見出しがある前提で測り直すこと。
