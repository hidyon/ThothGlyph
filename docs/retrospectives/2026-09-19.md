# 振り返り 2026-09-19

対象: [0001](../issues/0001-autosave.md) 自動保存、[0014](../issues/0014-unit-tests.md)
テスト土台、[0006](../issues/0006-math-in-code.md) コード内の `$`、
[0007](../issues/0007-preview-performance.md) プレビュー性能。
振り返るのはコードではなく進め方。

## 仕様が実装中に変わった箇所

3件のうち2件で、仕様に書いた前提が実装で崩れた。いずれも**実際に動かす前に
決め打ちした技術的前提**だった。

1. **0014**: 「`localStorage` のスタブで足りるのでjsdomは入れない」と書いたが、
   DOMPurifyの `sanitize` が `window` を要求し、node環境では動かなかった。
   → jsdomを足した。
2. **0006**: フェンスの判定を正規表現で書く前提だったが、複数行モードの `$` が
   行末にも当たるため「閉じないフェンスは末尾まで」が表せなかった。
   → 行単位の走査に変えた。
3. **0001**: 「初回レンダリングか」で初回保存を抑止する実装が、React StrictModeの
   二重マウントで破れた。→ 最後に保存した内容との比較に変えた。

**なぜ仕様の時点で気づけなかったか。** 3つとも「ライブラリや環境が実際にどう動くか」
であって、考えれば分かる類ではない。1行動かせば分かることを、仕様に断定で書いていた。

**提案**: 仕様に「確かめずに書いた前提」を明示する節を設ける。書いた時点で未確認の
ものは断定せず、実装の最初にそこから確かめる。CLAUDE.mdの仕様テンプレートに
`## 未確認の前提` を足したい。

## 受け入れ基準で捕まえられなかった問題

- **0001の受け入れ基準は全部通ったのに、初回訪問時にサンプルが保存される不具合が
  残っていた。** 検証スクリプトを書いて初めて気づいた。基準に「初回訪問では
  保存状態を出さない」を入れていたのが効いた——が、これは仕様を書くときに
  「保存しない」という**内部の状態**ではなく「表示が出ない」という**観測できる結果**で
  書いたから捕まえられた。内部状態で書いていたら通り抜けていた。
- 0007では受け入れ基準に**数値**（50ms以内、1.5秒以内）を入れた。実測29.2msと308msで、
  「速くなった気がする」で終わらずに済んだ。起票時に現状を実測してあったことが効いた。

**提案**: 「受け入れ基準は観測できる結果で書く」「性能の話は起票の時点で現状を測る」の
2つをCLAUDE.mdの仕様の節に足したい。

## 守られなかったルール / 明文化したい暗黙のルール

- **守られなかった**: 「issue着手時に `issue/NNNN-slug` ブランチを切る。`main` に
  直接コミットしない」は守れたが、**完了したブランチをどうするかが書かれていない**。
  0001をmainへマージしようとして権限で拒否され、0014のブランチを0001の上に積んだ。
  結果、4本のブランチが直列に伸びている。
  → マージの扱い（誰がいつやるか）をCLAUDE.mdに書きたい。
- **暗黙にやっていた**: 実装中に見つけた別の問題をissueにすること。今回は
  `insertSnippet` が既に選択範囲を包んでいた（0009の前提が誤り）ことが分かり、
  issueを書き直した。CLAUDE.mdには「新しいissueとして起票する」とあるが、
  「既存issueの前提が誤っていたら書き直す」は書かれていない。小さいので
  ルールを足すほどではないかもしれない。
- **暗黙にやっていた**: 検証スクリプトに受け入れ基準を1項目1チェックで足し、
  `OK/NG` と件数を出す形。0001で作ってから0006・0007でもそのまま使えた。
  CLAUDE.mdの「実機検証」に、この形を標準として書いておきたい。

## 削るべきルール

- CLAUDE.mdの構成図（`src/` のツリー）は、ファイルが増えるたびに更新が要る。
  今回2回更新した。`lib/` の中身は実際に見れば分かるので、**ツリーは
  ディレクトリの役割だけに減らせる**と思う。長いほど読まれなくなる。
- 「`@types/katex` は入れない」「Viteの `server.host` は消さない」は、どちらも
  一度踏んだ落とし穴の記録として今も有効。残す。

## CLAUDE.mdへの変更提案（2026-09-19 承認・適用済み）

1. 仕様テンプレートに `## 未確認の前提` を足す。
2. 「受け入れ基準は観測できる結果で書く」「性能は起票時に現状を測る」を追記。
3. 完了したissueブランチのマージの扱いを書く。
4. 実機検証の節に、`OK/NG` と件数を出す検証スクリプトの形を標準として書く。
5. `src/` のツリーを、ファイル単位からディレクトリの役割に減らす。

5点とも承認され、同日 `docs/claude-md-revision` で適用した。

---

# 振り返り（2回目） 2026-09-19

対象: CLAUDE.mdの改訂、[0016](../issues/0016-product-docs.md) プロダクト全体の
仕様文書、[0002](../issues/0002-dark-mode.md) ダークモード。

前回の振り返りから2件のクローズで回している（CLAUDE.mdの「3件ごと」より1件早い）。
前回入れたルールが効いたかどうかを見るには、早いほうがよいと判断した。

## 前回の変更は効いたか

### `## 未確認の前提` — 効いた。残す

0002で3つ挙げた（KaTeXのエラー色をCSSで上書きできるか、Playwrightで
`prefers-color-scheme` を切り替えられるか、ダークの色の値そのもの）。

- 2つは実装の冒頭に10分で確かめがついた。**仕様に書いてあったから最初に
  確かめた**のであって、書いていなければ実装の終盤まで気づかなかった。
- 1つ（初期表示のちらつき）は実測の結果、仕様の判断そのものを覆した。
  Reactのマウント時にテーマを当てる案から、`index.html` のインライン
  スクリプトに変えた（本番ビルドで白が20ms出た）。

前回の振り返りで「3件とも、考えれば分かる類ではなかった」と書いたことの裏返しで、
**確かめる場所を仕様の中に用意しただけで、確かめる順番が前に来た**。

### 受け入れ基準を観測できる結果で書く — 効いたが、抜け道を見つけた

0002の基準に「初期表示でちらつきが目に見えるかを確認し、**結果を報告する**」と
書いた。これは**落ちようがない基準**である。確認さえすれば「満たした」になる。

実際には測って報告し、そこから判断が変わったので結果は良かったが、それは
基準の書き方ではなく偶然による。**「報告する」で終わる基準は基準ではない。**
「測って、数値を仕様に記録する」まで書けば、記録がないことで落ちる。

→ CLAUDE.mdに1行足したい: 受け入れ基準の動詞は「確認する」「報告する」ではなく、
**残るもの**（数値、表示、ファイルの状態）で書く。

### 4文書をクローズ時に直す — 効いた。ただしコストは無視できない

0002で4文書のうち4つすべてに手が入った（要求にR5とN6、アーキテクチャにテーマの
保存とインラインスクリプトの例外、機能仕様にテーマの節、テスト仕様に件数と
「色の測り方」）。

書く手間はあったが、**アーキテクチャ文書に「`index.html` が唯一の例外」と
書けたことは、コードのコメントだけでは残らなかった**。例外は文書に書いてこそ
次に読む人に届く。ルールは維持でよい。

### マージのルール — 効いた

「クローズしたら `--no-ff` でマージしてブランチを消す」を入れた結果、
ブランチが直列に積み上がる状態は解消した。ただし**マージは毎回ユーザーに
聞いている**ので、指示の往復が1回増えている。これは意図した通りの設計
（コミットとpushと同じ扱い）なので、変えない。

## 新しく見つかった問題

### 検証スクリプトが育ちすぎている

`scripts/verify-ui.mjs` は377行・38チェック・実行21秒になった。
「前回までのチェックを消さない」という前回のルールの直接の帰結であり、
ルール自体は正しい（0002の変更で既存23件が壊れていないことを確認できた）。

ただしこのまま増え続けると、1つのissueを検証するのに全部を流すことになる。
**issueとして起票する**（[0017](../issues/0017-verify-script-structure.md)）。
CLAUDE.mdのルールは変えない。

### トークン消費がセッションの後半で跳ね上がる

[所要トークンの記録](../token-usage.md) を見ると、0006が約88万、0002が約875万。
issueの大きさの差ではなく、**同じセッションの何件目か**で決まっている。
会話が長いほど1往復あたりのキャッシュ読みが増えるため。

進め方の問題として言えば、「1セッションで何件も片付ける」のは楽だが高い。
ただしセッションを分けると文脈の引き継ぎが要る。どちらを取るかはユーザーの
判断なので、ルールにはせず、記録だけ残す。

## CLAUDE.mdへの変更提案（2026-09-19 承認・適用済み）

1. 受け入れ基準の動詞を「残るもの」で書く、を足した（上記の抜け道への対処）。

前回5点入れた直後なので、今回は1点だけにしている。**CLAUDE.mdは足すだけでなく
減らす**の原則からも、効果が確かめられていないルールを重ねたくない。

---

# 振り返り（3回目） 2026-09-19

対象: [0009](../issues/0009-wrap-selection.md) 選択範囲があるときの挿入。
あわせて、issueを3件起票したとき（0018・0019・0020）の進め方も見る。

## 数え方を間違えた

まず前提の訂正から。クローズ報告のときに「0002・0016に続く3件目のクローズ」と
言ったが、これは誤りだった。2回目の振り返りが0016と0002を対象にしているので、
**そこから数えれば0009は1件目**である。

CLAUDE.mdの「3件クローズするごとに」を守るなら、振り返りはまだ先だった。
1回目が4件、2回目が2件、3回目が1件。一度も「3件ごと」で回っていない。
数字が守られていないこと自体より、**毎回わたしが数え違えていること**が問題で、
数え始める起点（前回の振り返りの対象はどこまでか）が記録から読み取りにくい。

→ 各振り返りの冒頭に「対象」を書く運用は既にできている。数える起点はそこなので、
ルールを変えるより**振り返りを書く前に前回の「対象」を読む**を徹底すればよい。
今回それをやって気づいた。ルールの追加は要らないと判断する。

## 前回の変更は効いたか

### 受け入れ基準の動詞を「残るもの」で書く — 効いた

0009の受け入れ基準12項目に、「確認する」「報告する」で終わるものは1つもない。
すべて具体的な入出力か、DOMから読める値になっている
（`insertSnippet('x + 1', '\pi', 0, 5)` が何を返すか、`title` 属性が何を含むか）。

前回見つけた抜け道（確認さえすれば満たせてしまう基準）は塞がった。
1行足しただけのルールだが、**基準を書くときに動詞で引っかかる**のが効いている。

## 仕様が実装中に変わった箇所

なし。仕様に書いた通りに実装が通った。3回の振り返りで初めてである。

ただしこれを「仕様の質が上がった」と読むのは早い。0009は
**触る関数が1つ（`insertSnippet`）と決まっていて、既にテストがあった**。
0014でテストを入れ、0009の起票時に前提の誤りを潰してあったので、
仕様を書く時点で分からないことがほとんど残っていなかった。
条件が楽だっただけ、と見るのが妥当。

## `## 未確認の前提` が別の用途に使われた

0009でこの節に挙げた2つは、どちらも**確かめるつもりのない前提**だった。

- `execCommand('insertText')` なら履歴を保てるはずだが、採らない
- `aria-label` の読み上げは実地確認しない

これは本来の用途と違う。この節は1回目の振り返りで「**実装の最初にそこから
確かめる**ための場所」として入れたものであって、「確かめないと宣言する場所」
ではない。今回は実質「スコープ外の宣言」として使っていた。

害があったかというと、なかった。むしろ「なぜ確かめないか」が仕様に残ったのは
よかった。ただし節の名前と用途がずれたまま増えると、**実装の冒頭に確かめる
リスト**としての機能が薄まる。確かめるものと確かめないものが同じ箱に入っていると、
どれを先に潰すべきか分からなくなる。

→ 様子を見る。今回1件だけなので、ルールを足すほどではない。
次回また同じ使われ方をしたら、節を分ける（`## 未確認の前提` と
`## 確かめないと決めたこと`）ことを提案する。

## 受け入れ基準で捕まえられなかった問題

**検証チェックどうしが汚染し合っていた。**

0009の実機検証を足して43/43件OKになったが、出力をよく見ると、
「囲める記号は選択したテキストを包む」のチェックが
`\sqrt{# 二次方程}\alpha 式の解の` を見ていた。前のチェックで挿入した
`\alpha` が自動保存で復元され、その文書に対して次のチェックを流していた。

**チェックは通っていた**（包む挙動は確かに起きている）。しかし意図した条件では
なかった。基準の書き方の問題ではなく、**検証スクリプトが状態を持つ**ことの問題で、
0001で自動保存を入れた時点から潜在していたものが、初めて表に出た。

直した（保存の落ち着きを待ってからlocalStorageを消して開き直す）が、
気づいたのは `OK` の行に出した実際の値を読んだからで、件数だけ見ていたら
通り抜けていた。CLAUDE.mdに「スクリーンショットを実際に見る」はあるが、
**チェックが見ている値を読む**とは書いていない。

→ これは [0017](../issues/0017-verify-script-structure.md)（検証スクリプトの
区分け）に直結する。区分ごとに流せるようにするなら、**各区分は既知の初期状態から
始める**が要件になる。0017のメモに追記した。CLAUDE.mdは変えない。

## issueの背景を推測で書いた

0019（アプリのアイコン）を起票するとき、背景に「faviconがViteのロゴ
（`/vite.svg`）のまま」と書いた。**確かめずに書いて、間違っていた。**
実際の `public/favicon.svg` には紫のアスタリスク——Claudeのロゴが入っていた。
コミット前に開いて気づき、書き直した。

CLAUDE.mdは仕様について「確かめずに書いた技術的な前提は断定せず
`## 未確認の前提` に挙げる」と言っているが、**issueの背景**には何も言っていない。
そして背景の事実誤認は仕様より質が悪い。issueは「なぜやるか」の出発点なので、
ここが間違っていると、その上に積んだ仕様も受け入れ基準も的を外す。

今回は「Viteの足場だろう」という思い込みで、ファイルを1つ開けば分かることだった。
1回目の振り返りで書いた「1行動かせば分かることを、仕様に断定で書いていた」と
同じ失敗を、場所を変えて繰り返している。

→ **CLAUDE.mdに1行足したい**: issueの背景に現状を書くときは、
実際にファイルや画面を見てから書く。

## トークン

0009は約26万で、これまでで最も安い（0002の875万の1/33）。
2回目の振り返りで立てた「同じセッションの何件目かで決まる」という仮説どおり、
**新しいセッションの1件目**だった。issueの規模が小さいことも効いているが、
0006（セッション前半の小さな修正）が約88万だったことと比べても安い。

記録として [token-usage.md](../token-usage.md) に残した。ルールにはしない
（セッションを分ける手間との引き換えはユーザーの判断）。

## 削るべきルール

今回は見つからなかった。前回「効果が確かめられていないルールを重ねたくない」と
書いた方針は続ける。今回の提案も1点だけにしている。

## CLAUDE.mdへの変更提案（2026-09-19 承認・適用済み）

1. issueの背景に現状を書くときは、推測で書かず実際に見てから書く（上記の0019の件）。

以上1点。承認され、`### issue管理` に追記した。`## 未確認の前提` の節を分ける案と、振り返りの件数の数え方は、
どちらも**今回は様子見**として見送る。

---

# 振り返り（4回目） 2026-09-19

対象: [0026](../issues/0026-typescript-strict.md) TypeScriptをstrictにする。
あわせて、issueを9件起票したとき（0018〜0027）の進め方も見る。

前回（3回目）から**2件のクローズ**（0026のみ。0009は前回の対象）。
CLAUDE.mdの「3件ごと」には足りていない。数え方は前回訂正したとおりで、
今回はユーザーの指示で実施している。この「数字と実態のずれ」自体を
後半で扱う。

## 前回の変更は効いたか

### issueの背景は推測で書かない — 効いた。しかも波及した

前回入れた1行。今回0018〜0027の9件を起票するあいだ、背景に書く事実は
**すべて実際にファイル・ビルド出力・`tsc` の実行結果を見てから**書いた。
0019で踏んだ「faviconはViteのロゴだろう」という誤りは再発していない。

それだけでなく、**思っていたより広く効いた**。

0026では起票の時点で `npx tsc --strict --noEmit` を流し、エラー0件を確かめた。
その結果:

- issueの「やりたいこと」が「設定を入れるだけ」と確定した
- 仕様に「コードは変えない。**エラーが出たら実測が誤っていたので仕様に戻る**」と
  書けた（曖昧さが消えた）
- 実装が実際に設定2行で終わり、所要トークンが約12万（これまでで最安）になった

前回は「背景の事実誤認は仕様より質が悪い」という守りの理由で入れたルールだった。
実際には**起票時に測ると、その下流の仕様と実装が両方短くなる**という攻めの効果が
あった。CLAUDE.mdは性能issueについてだけ「起票の時点で実測する」と言っているが、
0026は性能issueではない。**測れるものは測っておくと得**という一般形かもしれない。

→ ただし今回1件の観察なので、ルールにするのは早い。次に同種の例が出たら提案する。

### 受け入れ基準を「残るもの」で書く — 応用が効いた

0026は設定を入れるだけなので、「`strict` が有効である」だけでは
**それが実際に型エラーを止めるか**を確かめたことにならない。そこで基準を

> `strictNullChecks` に違反するコードを一時的に足すと `npm run build` が落ち、
> **そのエラーメッセージが仕様の「## 検証の記録」に書かれている**

と書いた。記録が空なら落ちる。2回目に入れたルール（動詞は残るもので書く）が、
「確認する」を禁じるだけでなく**確かめ方そのものを設計させる**方向に働いた。

実際、書いたおかげで一歩踏み込めた。エラーが出ることだけでなく、
`--strict false` にすると同じコードが0件で通ることまで確かめ、
**止めているのが `strict` であって他のフラグではない**ことを記録できた。

## 仕様が実装中に変わった箇所

なし。2回連続。ただし0009と同じく、0026は**触る対象が設定ファイル2つと
決まっていて、起票時に実測済み**だった。条件が楽だったと見るのが妥当で、
「仕様の質が上がった」と読むのはまだ早い。

## 受け入れ基準で捕まえられなかった問題

### 検証が1回落ちたのに、何が落ちたか残っていない

0026のマージ直後、`node scripts/verify-ui.mjs` が **1回だけ 42/43** で落ちた。
その後12回（単独5回・ビルド同時7回）流して再現していない。

**受け入れ基準の問題ではない。** 基準は全項目を満たしていたし、落ちたのは
マージ後の確認である。問題はわたしの手順にあった:
**出力を `tail -2` で切っていたため、`NG` の行を捨てた。** 件数だけが残り、
どのチェックが落ちたかは永久に分からない。

3回目の振り返りで、検証チェックどうしの汚染を「`OK` の行に出した実際の値を
読んだから気づいた」と書いた。そのとき「CLAUDE.mdに『チェックが見ている値を読む』
とは書いていない」と指摘しながら、**ルールを足さずに0017のメモに回した**。
今回、同じ領域を別の形で踏んでいる。2回目なので、今度は明文化したい。

→ [0027](../issues/0027-verify-flaky.md) を起票した。スクリプト側に記録を
残す仕組みを作る話はそちらで扱う。CLAUDE.mdには**出力を切り詰めない**ことを書く。

## 守られなかったルール

### issue起票のブランチが作業ブランチの下敷きになった

`docs/issues-0021-0026`（起票だけ）を未マージのまま、その上に
`issue/0026-typescript-strict` を切った。1回目の振り返りで問題にした
「ブランチが直列に積み上がる」形の再発である。

CLAUDE.mdの `### git` は **issueの作業ブランチ**については書いている
（着手時に切る、クローズしたらマージ、ブランチを消す）が、
**issueの起票や文書だけの変更**をどう扱うかは書いていない。
結果、起票ブランチが宙に浮き、次の作業がその上に乗った。

実際このセッションでは `docs/issues-0018-0020`、`docs/issues-0021-0026`、
`docs/retrospective-3`、`docs/retrospective-3-applied` と、
作業ブランチ以外のブランチを4本作っている。どれも中身は文書1〜7ファイルで、
レビューの単位として意味があるわけではない。

→ **提案**: issueの起票や文書だけの変更は、ブランチを切ったら
**その場でマージまで済ませる**（作業を上に積まない）。

## 削るべきルール

### 「issueを3件クローズするごとに振り返る」の3件

実績は 4件 → 2件 → 1件 → 2件。**一度も3件で回っていない。**
3回目に「数え方を徹底すればよい」と結論したが、今回も2件で実施している
（ユーザーの指示による）。

守られない数字を掲げ続けると、ルール全体の重みが下がる。実態を見ると、
振り返りが起きているのは「区切りがついたとき」か「進め方に引っかかりが出たとき」で、
件数ではない。今回も、検証の揺れという引っかかりがあった直後である。

→ **提案**: 「3件クローズするごとに」を
「**issueを2〜3件クローズしたとき、または進め方に引っかかりが出たとき**」に緩める。
自分から切り出す義務は残す（これは効いている。3回とも自分から提案できている）。

CLAUDE.mdは足すだけでなく減らす、の観点では、**数字を1つ手放す**提案になる。

## トークン

0026は約12万で最安。[token-usage.md](../token-usage.md) に記録した。
ただしこれは作業量の差が大きく、「セッションの後半ほど高い」という
2回目の仮説を覆すものではない。同じセッションで0009（26万）→ issue起票9件 →
0026（12万）と進んでおり、後半のほうが安いのは**中身が軽いから**である。

## CLAUDE.mdへの変更提案（2026-09-19 承認・適用済み）

1. **検証の出力を切り詰めない。** 落ちたときは `NG` の行と実測値を必ず残す。
   （今回 `tail -2` で情報を捨てた。3回目の「値を読む」と同じ領域の2度目）
2. **issueの起票や文書だけの変更は、ブランチを切ったらその場でマージまで済ませる。**
   （作業ブランチの下敷きになるのを防ぐ。1回目に直したはずの形の再発）
3. **振り返りの頻度「3件クローズするごとに」を、「2〜3件、または進め方に
   引っかかりが出たとき」に緩める。**（4回とも守られていない数字を手放す）

1と2は足す、3は緩める。3点とも承認され、同日適用した（1は `## 実機検証`、
2は `### git`、3は `## 振り返り`）。

---

# 振り返り（5回目） 2026-09-19

対象: [0026](../issues/0026-typescript-strict.md) strict、
[0017](../issues/0017-verify-script-structure.md) 検証の区分け、
[0027](../issues/0027-verify-flaky.md) 検証の記録、
[0018](../issues/0018-formula-library.md) 公式の挿入。

前回（4回目）の対象は0026だったので、そこから数えると**3件**（0017・0027・0018）。
新しい頻度のルール（2〜3件、または引っかかりが出たとき）に収まっている。
今回は0018のクローズ時に自分から切り出し、ユーザーの指示で実施した。

## 前回の変更は効いたか

### 出力を切り詰めない — 効いた。ただし「効いた」と言い切れない

4回目で `tail -2` を使って `NG` の行を捨てた反省から入れた。今回、検証の結果を
見るときは `grep -E "^NG|件 OK"` のように**NGの行を必ず含む形**にした。

ただし今回は一度もNGが出ていない。**ルールが働く場面がまだ来ていない**ので、
効いたかどうかは本当のところ分からない。効いたのは「切り詰めない書き方をした」
ところまでで、そこから先は次にNGが出たときに分かる。

なお同じ問題に対して [0027](../specs/0027-verify-flaky.md) でスクリプト側にも
手を入れた（NGの再掲とJSON出力）。**人の規律とスクリプトの仕組みの両方で
押さえた**形で、ルールだけに頼らずに済んでいる。こちらのほうが本筋だと思う。

### 起票・文書だけの変更はその場でマージ — 効いた

4回目以降、`docs/retrospective-4` も `issue/0027` も、起票や文書だけのブランチを
宙に浮かせていない。ブランチが直列に積み上がる形は再発していない。

### 振り返りの頻度を緩めた — 判断しづらい

「2〜3件、または引っかかりが出たとき」に変えたが、今回は3件目で、かつ
ユーザーの指示があった。**自分から切り出す**ほうは0018のクローズ時にできている。
緩めたこと自体の効果は、もう数回見ないと分からない。

## 仕様が実装中に変わった箇所

### `CURSOR_TOKEN` の置き方（0018）

仕様には「解の公式なら `x` の位置に置く」と書き、記号を**置き換える**形を
想定していた。実装中に、置き換えるとトークンを取り除いた文字列が壊れる場合が
あることに気づいた。

```
%CURSOR%^2 = a^2 + b^2 - 2ab \cos C   →  トークンを消すと ^2 = … で壊れる
```

**直前に置く**形に変えた。トークンを消した文字列が常に元の式のままになる。

なぜ仕様の時点で気づけなかったか。仕様を書くとき、解の公式（`x = …`）だけを
見て決めていた。30件のうち1件で成り立つ置き方を、全体の規則として書いていた。
**データを1件だけ見て規則を決めると、残りで破れる。**

→ 大きな損はなかった（実装中に気づき、方針を変えただけ）。ただし
「代表例1つで規則を決めない」は、次に同じ形のissue（データを並べるもの。
[0011](../issues/0011-palette-search.md) や収録範囲を増やす類）で効くはず。
ルールにするほどではないが、覚えておく。

## 受け入れ基準で捕まえられなかった問題

### 自分で書いた基準が矛盾していた（0027）

> わざと失敗させた変更を戻すと、再び43/43件OKになり `git status` がクリーンになる

**0027自身の変更がコミットされていない段階で、クリーンになるはずがない。**
基準を書いた時点で気づけた矛盾で、検証のときに初めて気づいた。

実害はなかった（確かめたかったのは「わざと落とすための書き換えが残っていないこと」で、
それは確認できた）。ただし**基準が現実に照らして成立するかを、書いた直後に
一度読み返していない**ことの表れではある。

2回目の振り返りで「落ちようがない基準」を問題にしたが、今回は逆に
**満たしようがない基準**を書いた。どちらも「基準を書いたあとに読み返す」で
拾える種類のもの。

→ 提案するかどうか迷う。「基準を読み返す」は当たり前すぎてルールにすると
形骸化しやすい。**今回は提案しない。**次に同種の矛盾が出たら明文化する。

## 良かったこと

### 仕様を2本まとめて書いて実装を分けた（0017 + 0027）

同じファイルを触る2つのissueを、**設計はまとめて・実装は分けて**進めた。
結果、`verify-ui.mjs` を2回書き換えずに済み、`1 issue = 1マージコミット` も保てた。

0027の仕様は0017で入れる区分構造を前提に書いてあり、実装は上に乗せるだけだった。
**依存する2件を続けてやるときの型**として使える。CLAUDE.mdに書くほど一般的か
まだ分からないので、記録にとどめる。

### 機械でしか確かめられないものをテストに寄せた（0018）

公式30件のLaTeXは、目で見ても正しいか分からない。全件を `renderMarkdown` に
通して `katex-error` が出ないことをテストにした（124件）。
実際には1件も落ちなかったが、**落ちなかったことが確認できた**のが価値である。

### 未確認の前提を3つとも実測した（0018）

パレットの高さ（94→148px）、幅600pxでの折り返し（折り返さない）、
KaTeXのエラー（なし）。**数字が仕様に残った**ので、次に「パレットが高すぎる」
という話が出たときに、どこから増えたかが分かる。

## 削るべきルール

見つからなかった。前回「3件ごと」の数字を手放したばかりで、まだ効果を見ている
途中である。

## CLAUDE.mdへの変更提案

**なし。**

今回見つけたもの（代表例1つで規則を決めない、基準を書いたら読み返す）は、
どちらも**1回しか起きていない**。4回目の振り返りで、同じ領域を2度踏んでから
明文化する形を取った（出力の切り詰め）。その基準に照らすと、今回はまだ早い。

CLAUDE.mdは足すだけでなく減らす。**足さない回があってよい。**

---

# 振り返り（6回目） 2026-09-19

対象: [0029](../issues/0029-greek-complete.md) ギリシャ文字、
[0030](../issues/0030-formula-library-more.md) 公式の追加、
[0031](../issues/0031-english-ui.md) 英語対応。

前回（5回目）の対象は0018までだったので、そこから**3件**。頻度のルールに収まっている。
0031のクローズ時に自分から切り出し、ユーザーの指示で実施した。

## 前回の宿題への答え

### 「出力を切り詰めない」は、今回はじめて働く場面が来た

5回目に「ルールが働く場面がまだ来ていないので、効いたかどうかは分からない」と
書いた。今回**実際にNGが1件出た**（`幅600pxでも横スクロールが出ない`）。

`tail -35` で見ていたが、スクリプト側が[0027](../specs/0027-verify-flaky.md)で
入れた「落ちたチェックの末尾再掲」を出すので、NGの行と区分名が残った。
**人の規律ではなくスクリプトの仕組みのほうが効いた。**5回目に「こちらのほうが
本筋だと思う」と書いたのは当たっていた。

### 「代表例1つで規則を決めない」は再発しなかった

0031で182件を翻訳するとき、ギリシャ文字（`alpha` のように日英が同じ）と
数学用語（`余弦定理` → `Law of cosines`）で扱いが違うことに、規則を書く前に
気づけた。異体字（`varpi（piの別の形）`）も別扱いにした。5回目の記録が効いたのか、
データを眺めてから書いたからかは区別できない。

## 仕様が実装中に変わった箇所

### ツールバーのCSS（0031）— 仕様の対象ファイルになかった

言語ボタンを1つ足した結果、**幅600px・日本語・保存状態ありの組み合わせで
ツールバーが45px溢れ**、既存チェックが落ちた。仕様の対象ファイル表に
`src/index.css` は挙げていなかった。

なぜ仕様の時点で気づけなかったか。仕様では「ツールバーにボタンを足す」ことしか
見ておらず、**そのボタンが占める幅**を見ていなかった。画面はゼロサムで、
要素を1つ足せば他の要素の余白が減る。0031の仕様には「翻訳の量」の表はあったが、
「増える要素の幅」の数字はなかった。

[0018](../specs/0018-formula-library.md)では「未確認の前提」にパレットの高さを挙げ、
94→148pxと実測して仕様に残した。**同じことをツールバーの幅でやるべきだった。**
0018でやったことが0031で抜けたのは、0018が「パレットが高くなる」と分かっていたのに対し、
0031は翻訳が主題でレイアウトは付随だと思っていたため。

→ **提案**: 「画面に要素を足すissueでは、その要素が占める幅・高さを仕様の時点で
測って書く」。これは0018で1回、0031で1回（抜けて事故になった）の**2回目**なので、
明文化の基準（同じ領域を2度踏んでから）を満たす。

### 3状態から2状態へ（0031）— 仕様の段階で直った

言語を `auto / ja / en` の3状態で書いていた。理由は「テーマ（0002）と同じ形に
そろえる」。ユーザーの一言で2状態になり、そこではじめて
**「テーマの `system` はOSの設定が使用中に変わるから追従に意味があるが、
利用者の読む言語は使用中に変わらない」**という本質的な理由が出た。

**「既存の形にそろえる」は、それ自体では理由にならない。** そろえる先の形が
なぜその形なのかを見ないと、理由のない対称性を作ってしまう。実装前だったので
手戻りはゼロ。CLAUDE.mdの「仕様が複数の解釈を許すなら、実装ではなく質問で解消する」が
効いた場面でもある（今回は私が聞き、ユーザーが即断した）。

→ ルールにするほどではない。記録にとどめる。

## 受け入れ基準で捕まえられた問題

**「既存59件の検証チェックが通ったままである」が回帰を捕まえた。** これが
基準になければ、`i18n` の18件が全部OKなのを見て完了と判断していた。

CLAUDE.mdの「前回までのissueのチェックは消さない（回帰に気づける）」が、
受け入れ基準の形で効いた。**チェックを残すだけでなく、基準に1行書く**のが効く。

NGが出たあと、`git stash` で実装を退避して変更前の状態で同じ区分を流し、
**回帰だと確認してから直した**。「自分の変更が原因か、もともと落ちていたか」を
先に切り分けると、直す場所を間違えない。

## 良かったこと

### 同じ形の編集がN件あるとき、N回編集せず1回の変換として書いた（0031）

182件の翻訳を、対応表を持ったPythonスクリプトで一括変換した。所要トークンは
約11万で、0030（18万・公式30件）より安い。**翻訳の量が消費にほとんど乗らなかった。**

### 翻訳漏れを型ではなく テストで捕まえる形にした（0031）

`t('平方根', '平方根')` は型が通ってしまう。全件が日英の両方を空でなく持つことと、
日英が同じ文字列のもの（ギリシャ文字24件）の**数を固定**するテストを入れた。
記号を足して英語を書き忘れると、24件が25件になって落ちる。

## 守られなかったルール

CLAUDE.mdは「実装中に『ついでに直したい』ものを見つけたら、直さずに新しいissueとして
起票する」「仕様の前提が崩れたら、実装を続けずに仕様に戻って相談する」と書いている。

CSSの件は**どちらにも当てはまらない**（ついでではなく自分が起こした回帰、
前提が崩れたわけでもない）。相談せずに直し、仕様に「実装中に仕様から外れたこと」として
記録した。判断は妥当だったと思うが、**ルールには書かれていない三つ目の場合**である。

→ **提案**: 「自分の変更が起こした回帰は、その場で直して仕様に記録する」を1行足す。
起票すると、壊れたまま次のissueに持ち越すことになる。

## 削るべきルール

**CLAUDE.mdと `docs/` の4文書が重複している。** 実際に突き合わせた結果:

| CLAUDE.mdの節 | 行数 | 重複先 |
|---|---|---|
| 設計上の約束（7項目） | 14 | `docs/architecture.md`（7項目すべて。より詳しい理由つき） |
| 検証の使い分け | 9 | `docs/test-spec.md` §1（同じ表がある） |
| 実機検証 | 26 | `docs/test-spec.md` §3（手順・`executablePath`・1項目1チェック・切り詰めない・`aligned` まで同じ） |
| `src/` のディレクトリツリー | 10 | `docs/architecture.md` §2 |

[0016](../specs/0016-product-docs.md)で4文書を作ったとき、CLAUDE.mdから移さなかった。
結果、**同じことが2か所にあり、片方だけ古くなる形**になっている。1回目の振り返りで
「ツリーはファイルが増えるたびに更新が要る」と書いたのと同じ問題が、範囲を広げて残っている。

## CLAUDE.mdへの変更提案

### 1. 重複している4か所を、ポインタ1行に置き換える（−59行）

上の表の4か所を消し、代わりに「いつ読むか」を書いた行を残す。

```markdown
### 設計と検証の詳しい取り決め

- コードに手を入れる前に [docs/architecture.md](docs/architecture.md) を読む。
  変換の順序、サニタイズの境界、入れない依存（`@types/katex` など）の理由がある。
- 受け入れ基準を書く前・検証する前に [docs/test-spec.md](docs/test-spec.md) を読む。
  単体テストと実機検証の使い分け、検証スクリプトの書き方がある。
```

**この提案の危うさ**: CLAUDE.mdは毎回読み込まれるが、`docs/` は読みにいかないと
読まれない。消した瞬間に「数式はMarkdown変換の前に退避する」のような**罠**が
視界から消える。ポインタに「いつ読むか」を書くのはそのためだが、
読み飛ばされれば同じである。

ただし今回、`docs/` の4文書はissueのたびに更新しており（0031でも4文書すべて直した）、
**中身が古くなる心配はCLAUDE.md側のほうが大きい**。2か所にあるうち
更新されるほうを残す、という判断になる。

### 2. 今回見つけたものを2行足す（+2行）

- 仕様の節に: 「**画面に要素を足すissueでは、その要素が占める幅・高さを
  仕様の時点で測って書く。** 画面はゼロサムで、足した分だけ他の余白が減る」
  （0018で実測して効き、0031で抜けて回帰した）
- 実装の節に: 「**自分の変更が起こした回帰は、その場で直して仕様に記録する。**
  起票すると壊れたまま次のissueに持ち越すことになる」

### 差し引き（適用後の実績）

**191行 → 159行（−17%）。** 見積もりは134行だったが、ポインタに「いつ読むか」と
「読まずに触ると壊す」を書いたぶん（12行）と、新しいルール2つ（7行）で戻った。

適用時に**もう1か所の重複**が見つかった。独立していた「## 検証」節が、
サイクルの「4. 検証する」と同じこと（型が通ることは検証ではない）を書いていた。
`docs/` との重複を消したら、CLAUDE.md**内部**の重複が見えた。節を畳んで
「詳しい取り決めはどこにあるか」に吸収した。

消したルールが `docs/` に本当に残っているかは、キーワード12個
（`throwOnError`・`MathML`・`データ駆動`・`@types/katex`・`server.host`・`it.fails`・
`executablePath`・`1項目1チェック`・`終了コード`・`切り詰め`・`aligned`・`console`）で
照合した。**全件が `docs/architecture.md` か `docs/test-spec.md` にある。**

**足すより減らすほうが多い回**にできた。5回目は「足さない回があってよい」で
終わったが、今回は「減らす回」になった。
