# テスト仕様

> **更新のタイミング**: 検証の手段や分担が変わったとき、テストの本数が変わったとき。
> issueをクローズしたら、件数と一覧を実際の出力に合わせる。

## 1. 何をどちらで確かめるか

3つの手段がある。**どれで確かめるかは、issue仕様の受け入れ基準を書くときに
項目ごとに決める。**

| 手段 | コマンド | 守る範囲 | 向くもの |
|---|---|---|---|
| 単体テスト（Vitest） | `npm test` | `src/lib/` の純粋関数 | 入出力の網羅、エッジケース、回帰の固定 |
| 実機検証（ヘッドレスChromium） | `node scripts/verify-ui.mjs` | 画面と操作の往復 | 描画、入力、保存、レイアウト、描画の性能 |
| 読み込みの実測（本番ビルド） | `node scripts/measure-load.mjs` | 成果物の大きさと初期表示までの時間 | バンドルの分割、読み込みの性能 |

**読み込みの時間は `verify-ui.mjs` では測らない。** 開発サーバはバンドルされて
いないので、測っても本番の値にならない（[0024](specs/0024-bundle-size.md)）。

**型が通ることは検証ではない。** `npm run build`（`tsc -b` + vite build）と
`npm run lint`（oxlint）は前提であって、受け入れ基準の確認ではない。
その型チェックは `strict` で走る（[0026](specs/0026-typescript-strict.md)）ので、
前提の強さは `strict` の範囲まで。そこから先は上の3つで確かめる。

境界の引き方: 文字列を入れて文字列が返るものはテスト、DOMに出て初めて確かめられる
ものは実機。`renderMarkdown` の出力は文字列なのでテスト、その文字列が
ブラウザでどう見えるかは実機。

## 2. 単体テスト

### 置き場所と書き方

対象の隣に `*.test.ts` として置く（`src/lib/renderMarkdown.test.ts`）。
`tests/` ディレクトリは切らない。`it()` の説明は日本語で「何が起きるか」を書く。

環境は既定の node。ただし `renderMarkdown.test.ts` だけは jsdom で動かす
（DOMPurifyの `sanitize` が `window` を要求するため、ファイル先頭に
`// @vitest-environment jsdom`）。`documentStorage.test.ts` は `window.localStorage` を
スタブする（差し替えるのは `getItem` / `setItem` / `removeItem` の3つだけ。
`removeItem` は0065の読み継ぎが旧キーを消すため）。
`langStorage.test.ts` は `navigator.language` もスタブする（既定の言語が
実行環境で変わると結果が変わるため）。

### 既知の不具合の書き方

直せない・直さない不具合は `it.fails()` で「いま失敗すること」を書き残す。
直したときに `it()` へ裏返す。放置された不具合が一覧に見えるようにするため。
[0006](specs/0006-math-in-code.md) はこの形で引き継いだ（0014で2件記録 → 0006で裏返し）。

### 現在のテスト（907件）

| ファイル | 件数 | 守っていること |
|---|---|---|
| `insertSnippet.test.ts` | 16 | カーソル位置の計算、選択範囲を包む／選択の直後に挿入する、空文書、**置き換える範囲と入れる文字列**（0021） |
| `palette.test.ts` | 327 | 選択範囲を囲めるかの判定、tooltipの文言、ギリシャ文字の件数と小文字24文字の網羅、**パレット全件のLaTeXがKaTeXで描画できること**（Markdownグループの `snippet` はLaTeXではないので対象外） |
| `formulas.test.ts` | 307 | 15分類75件であること、0018で合意した既存6分類が変わっていないこと、名前の重複、**全75件のLaTeXがKaTeXでエラーにならないこと**、CURSOR_TOKENがちょうど1つ、ブロック数式の形 |
| `renderMarkdown.test.ts` | 41 | インライン／ブロック数式、`$$`の優先、通貨表記、`\$`、強調に壊されないLaTeX、壊れたLaTeX、サニタイズ、コード内の`$`、キャッシュ、**graphフェンスのSVG化**（`graphql` を巻き込まないこと、ブロック内の`$`、エラーのエスケープ、言語、複数個）（0037）、**トップレベルのブロックに付く `data-line`**（退避で縮んでもずれないこと、`li`・`td`に付かないこと、KaTeXの出力に出ないこと、参照リンクが壊れないこと）（0010） |
| `expression.test.ts` | 10 | グラフの式の評価（優先順位、`^`の右結合、単項マイナス、暗黙の乗算、定数と関数、小数）、読めない式をエラーとして返すこと（例外を投げない）（0037） |
| `graphBlock.test.ts` | 15 | `y =` と `x:` `y:` の分解、既定の範囲、範囲に式を書けること、空行とコメント、3本まで、同じ指定の重複、エラー6種の文言（0037） |
| `renderGraph.test.ts` | 11 | 曲線のパス、`1/x`・`tan(x)` で線が切れること、定義域の外を描かないこと、定数関数、凡例の有無、目盛りの数と表記、軸の有無、aria-labelの言語、描ける点がないときのエラー、yの範囲の決め方（0037） |
| `documentStorage.test.ts` | 14 | 保存と復元、キーなし、壊れたJSON、版違い、型違い、例外を投げる環境、**旧キー `matheditor:` からの読み継ぎ**（0065） |
| `themeStorage.test.ts` | 13 | 保存と復元、壊れた値・版違い・知らない値の既定値、例外を投げる環境、巡回順、表示名（日英）、**旧キーからの読み継ぎ**（0065） |
| `langStorage.test.ts` | 18 | `navigator.language` からの既定の決め方、保存と復元、壊れた値・版違い・知らない値、保存が既定より優先されること、例外を投げる環境、往復、表示名、**旧キーからの読み継ぎ**（0065） |
| `loadMarkdownFile.test.ts` | 14 | 受け入れる拡張子と断る条件、1MBの境目、断る順番、BOM・CRLF・CRの正規化（0012） |
| `findMatches.test.ts` | 16 | 一致の列挙（重なりを数えない・大文字小文字の区別・空クエリ）、改行をまたぐ語、次/前の回り込み、カーソル位置からの再開、1件の置換、すべて置換（全文を1回で差し替える形・増殖しない・0件ならnull）（0043） |
| `highlightRanges.test.ts` | 10 | 地の文と一致の切り分け、currentが1つだけになること、文字を落とさないこと、末尾の改行を足すこと（pre-wrapでは行にならないため）、`&` `<` `>` のエスケープ（0043） |
| `downloadName.test.ts` | 17 | 見出しからのファイル名、使えない文字の置き換え、50文字で切る、末尾の改行、空白だけの判定（0034） |
| `lineMap.test.ts` | 13 | 行頭オフセットの配列、オフセットから行番号（行頭・行中・改行・範囲外）、退避後の位置から元ソースの位置への引き直し（0010） |
| `scrollMap.test.ts` | 18 | 行番号でのアンカーの突き合わせ、補間（組の間・手前・後ろ・0件・同値）、0〜toEndに収まること、逆方向、**スクロールで届かないアンカーを落として端どうしを合わせること**（0010） |
| `mirrorHtml.test.ts` | 8 | アンカー行への span の挿入、末尾の改行、`&`・`<` のエスケープ、行数が変わらないこと（0010） |
| `paneSizes.test.ts` | 20 | 3つの領域の幅の下限と既定値、割合への正規化、保存と復元、壊れた値（0057）、**旧キーからの読み継ぎ**（0065） |
| `search.test.ts` | 14 | コマンド名・日英の名前・labelの部分一致で引けること、前方一致が先に来ること、公式も対象であること、空／不一致、上限40件と残数、重複なし、**0064で足した語で引けること**（「正規分布」→`\mathcal{N}`、「covariance」→記号と公式の両方、「ド・モルガン」、0064の背景で0件だった語） |
| `i18n.test.ts` | 5 | `pick` の取り出し、**記号・公式・画面の文言の全件が日英の両方を空でなく持つこと**、日英が同じ文字列のもの（ギリシャ文字24件）の数の固定 |

網羅率は測っていない。件数ではなく「この関数が壊れたら気づけるか」で判断する。

## 3. 実機検証

### 手順

```bash
npm run dev                  # 別ターミナルで起動しておく
node scripts/verify-ui.mjs   # 全区分。スクリーンショットは tmp/screenshots/ に出る
node scripts/verify-ui.mjs theme        # テーマ区分だけ
node scripts/verify-ui.mjs theme perf   # 複数指定
```

**区分を選んで流せる**（[0017](specs/0017-verify-script-structure.md)）。
引数なしなら全区分なので、回帰の確認はこれまで通り。知らない名前を渡した場合は
使える名前を出して終了コード1で終わり、チェックは1件も実行しない。

区分はどれから流しても同じ結果になるよう、実行前に毎回初期状態へ戻す
（保存された文書とテーマを消し、OSのテーマ設定と画面幅を既定に戻す）。
**表示言語は日本語に固定して流す。** 既存のチェックの多くが日本語の文言で要素を
探しており、ヘッドレスChromiumの `navigator.language` は英語なので、放っておくと
英語で表示されて全部落ちる。固定は `addInitScript` で、保存がないときだけ
`ja` を置く形にしてある（`i18n` 区分が英語を保存したら、そちらが優先される）。
消す前に保存のデバウンス待ちを片付けるのは、保存が飛んだままリロードすると
`beforeunload` で書き戻されるため。

Chromiumはコンテナに焼いてあるので準備は要らない。`chromium.launch()` に
`executablePath` は**書かない**（`PLAYWRIGHT_BROWSERS_PATH` から
playwright-coreが自力で見つける。パスをベタ書きすると環境が変わった瞬間に嘘になる）。

### スクリプトの形

- 受け入れ基準は**1項目1チェック**で足し、`OK` / `NG` と「n/m 件」を出す。
- 1件でも落ちたら終了コードを1にする。コンソールエラーがあっても1にする。
- **前回までのissueのチェックは消さない。** 回帰に気づけなくなる。
- スクリーンショットは撮るだけでなく**実際に見る**。見た目の違和感は目視で
  判断せず、DOMの座標や値を測って確かめる（`aligned` の桁揃えは目の錯覚で
  崩れて見えることがある）。
- **画像も目視で判定しない。** アイコンが16pxで潰れていないかは、canvasに
  描いてインク量を数える（[0019](specs/0019-app-icon.md)）。ただし
  **faviconがタブにどう出るかはページのDOMの外**なので機械では見られない。
  そこは手元のブラウザで見る、と決めて仕様に書く。

### 現在のチェック（413件）

区分名はコマンドラインで打つ名前。所要時間は単独で流したときの実測。

| 区分 | 名前 | 件数 | 時間 | 内容 |
|---|---|---|---|---|
| 初期表示 | `initial` | 2 | 1.1秒 | サンプル文書のグラフが1つ描かれること（[0040](specs/0040-sample-graph.md)）。見出し・ブロック数式・数式の総数は出すだけで判定していない（[0028](issues/0028-initial-section-checks.md)） |
| 自動保存（[0001](specs/0001-autosave.md)） | `autosave` | 15 | 5.3秒 | 初回訪問、保存中→保存済みの表示、リロードでの復元、デバウンス中のリロード、壊れたJSON、保存失敗の表示、サンプルに戻す（OK／キャンセル） |
| コード内の`$`（[0006](specs/0006-math-in-code.md)） | `code-math` | 3 | 1.5秒 | `code` 要素の中にKaTeXの出力がない、コード外の数式は描画される、`$` が文字として残る |
| レイアウト（[0033](specs/0033-toolbar-overflow-phone.md)） | `layout` | 10 | 1.4秒 | 幅600pxでコピーボタンが画面内に収まり文言が長いほうで出る、幅360pxで横スクロールが出ない・4ボタンすべてが収まる・文言が「コピー」になる・押すとコピーできる、幅375pxでツールバーが1行のまま・textareaが207px以上 |
| 性能（[0007](specs/0007-preview-performance.md)） | `perf` | 5 | 8.2秒 | 400節・400数式での1文字あたりの入力反映、`更新中…` の出現と消滅、数式が最後まで描画される |
| テーマ（[0002](specs/0002-dark-mode.md)） | `theme` | 15 | 3.8秒 | OS追従、手動の巡回、リロード後の保持、自動への復帰、コントラスト比の実測、初期表示のちらつき |
| 選択範囲の挿入（[0009](specs/0009-wrap-selection.md)） | `selection` | 5 | 2.7秒 | 単体記号で選択が消えない、挿入後のカーソル位置、囲める記号が選択を包む、tooltipの文言 |
| ギリシャ文字（[0029](specs/0029-greek-complete.md)・[0055](specs/0055-greek-uppercase-complete.md)） | `greek` | 14 | 6.9秒 | 小文字31件・大文字24件、`\xi` を数式の中に挿入してξとして描画される、`\Alpha` がエラーなく入り式のソースに残ること、24個すべてクリックが自分に当たること、`Alpha` での検索、幅1199/720/360pxでの高さ（0032の蓋）、幅1440pxの縦帯で帯の高さが変わらず横スクロールも出ないこと |
| 公式の挿入（[0018](specs/0018-formula-library.md)・[0030](specs/0030-formula-library-more.md)） | `formula` | 11 | 3.6秒 | 2段目タブが15分類出る、分類ごとの件数、名前と式の両方が出る、tooltip、`$$`で囲まれて入る、プレビューが1つ増える、エラーが出ない |
| アイコン（[0019](specs/0019-app-icon.md)） | `icon` | 14 | 1.5秒 | 6ファイルが200、Claudeのロゴの紫が残っていない、`<text>`と外部参照を持たない、manifestの宣言、PNG4件の大きさ、16pxの白インク量と512pxとのずれ、コントラスト比、apple-touch-iconの四隅が不透明、ツールバーの掲出（`favicon.svg` 参照・20px）、幅600pxで文字が隠れて溢れない |
| 記号の検索（[0011](specs/0011-palette-search.md)） | `search` | 20 | 5.0秒 | 検索欄がタブ行に同居すること（幅1280/720/600でのパレットの高さ）、40件当たったときのパレットの占有率と結果欄のスクロール、横断検索でタブが選択されないこと、挿入と描画、選択範囲を囲めること（0009の回帰）、キーボードだけでの挿入、Escapeでタブに戻ること、一致なしの文言、英語表示 |
| 英語対応（[0031](specs/0031-english-ui.md)） | `i18n` | 20 | 4.6秒 | 日英の往復、`<html lang>` の一致、切り替えても文書が変わらない、ツールバー・ペイン・パレット・公式名が英語になる、tooltipの語順、リロード後の保持、保存がないときのサンプル文書 |
| 読み込みの分割（[0024](specs/0024-bundle-size.md)） | `loading` | 14 | 3.6秒 | エンジンが届かない間の入力・自動保存・パレットからの挿入、`準備中…` の表示（日英）、ラベルがLaTeXのソースで出ること、届いた後の描画、幅600pxでパレットの高さが変わらないこと |
| ファイルの読み込み（[0012](specs/0012-file-load.md)） | `file-load` | 32 | 49.4秒 | ボタンの置き場所、幅1440/600/375/360pxでの見出し行の高さとtextareaの高さ、確認のキャンセルとOK、プレビューへの反映、リロードでの復元、ドロップ（枠の変化・大きさが変わらないこと）、断る3つ（拡張子・1MB超・2件同時）、CRLFとBOMの正規化、0007と同じ規模（26.9 kB）の読み込み、英語表示 |
| パレット挿入のUndo（[0021](specs/0021-undo.md)） | `undo` | 18 | 13.8秒 | 挿入後のCtrl+Z、Ctrl+Shift+Z／Ctrl+Yでのやり直し、打鍵と挿入が混ざった履歴の戻り順、囲んだ選択範囲の復元、公式・検索結果からの挿入、Undo後のプレビューと自動保存 |
| ファイルの書き出し（[0034](specs/0034-file-save.md)） | `file-save` | 19 | 18.9秒 | ボタンの置き場所と並び（0043で3つ目が増えた）、幅1440/600/375/360pxでの見出し行とtextareaの高さ、サンプルの書き出し（名前と中身）、見出しなし・使えない文字・末尾の改行、空白だけの文書を断ること、書き出し→読み込みの往復、英語表示 |
| 関数のグラフ（[0037](specs/0037-graph.md)） | `graph` | 22 | 13.0秒 | SVGとして描かれること、幅1440pxで480×320pxに収まること、幅540/375/360pxでペインに収まり横スクロールが出ないこと、目盛りの実寸が11px以上で左端からはみ出さないこと、3本の色分けと凡例、壊れたブロックの赤字と本文が残ること、エラーの文字とダークの線のコントラスト、パレットからの挿入とCtrl+Z、グラフ3つを混ぜた長文での入力コスト、英語のaria-label |
| 検索・置換（[0043](specs/0043-find-replace.md)） | `find` | 44 | 16.4秒 | 見出し行のボタンとCtrl+Fでの開閉、フォーカス、件数と選択の移動、端での回り込み、0件の表示とボタンの無効化、1件の置換、すべて置換とCtrl+Z 1回での復帰、大文字小文字の区別、一致がすべて塗られること・現在の1件だけ色が違うこと・層が本文を覆わないこと・折り返しが一致すること・スクロールへの追従、Escで閉じてエディタへ戻ること、選択文字列が初期値になること、幅375/360pxでのバーの高さ・textareaの高さ・横スクロール、英語表示、400節の文書での入力コスト |
| スクロールの同期（[0010](specs/0010-scroll-sync.md)） | `scroll` | 20 | 25.7秒 | 節20・節36・ブロック数式の直後の見出しを上端に合わせたときのプレビューのずれ（32px以内）、最上端・最下端が合うこと、逆方向（プレビュー→エディタ）とその最下端、動かしたあと600ms待って位置が動かないこと（ループしない）、カーソルと選択範囲が変わらないこと、ミラーが見えず場所も取らないこと、折り返しのある文書、幅1199px・360px、400節での同期の所要 |
| 領域の幅の可変（[0057](specs/0057-resizable-panes.md)） | `panes` | 17 | 11.9秒 | 仕切り2本が6pxであること、ドラッグでパレット・ソース・プレビューの幅が変わること、リロード後も残ること、下限（150/360/360px）を割らないこと、下限でグラフが312px以上・見出し行が30pxのままであること、ダブルクリックで既定に戻ること、左右キーで16pxずつ動くこと、幅1199px・360pxで仕切りが出ないこと、壊れた保存値で既定に落ちること、400節の文書での操作コスト |
| パレットの置き場所（[0054](specs/0054-palette-placement.md)・[0056](specs/0056-palette-header.md)） | `placement` | 29 | 14.0秒 | 幅1440pxで左の縦帯（180px・ペインと同じ高さ）になること、textarea 810px以上・プレビュー625px以上・グラフ480×320のまま、タブ8つが縦に並びアイコンが出ること、どのボタンも中心のクリックが自分に当たること、公式タブで帯が縦スクロールして最後の公式まで押せること、縦帯での検索と挿入、ダークでの仕切り、幅1200pxの境目、幅1199pxで横帯に戻ること、パレットの見出しが縦帯でだけ出てソース・プレビューと同じ30pxであること、幅360pxで0032の蓋のまま、英語表示、サンプル文書から「上の」が消えていること |
| タブのアイコン（[0053](specs/0053-palette-tab-icons.md)） | `tab-icon` | 12 | 5.0秒 | 8タブすべてにアイコンがあること・8つが互いに異なること・空でないこと、aria-hiddenでタブの名前に混ざらないこと、幅1440px/360pxでパレットの高さが変わらないこと、英語表示でも同じ記号、ダークでラベルと同じ色、幅945pxでタブ行が1行のまま、幅360pxの横スクロールが751px以下、タブの切り替えが効くこと |
| パレットの高さ（[0032](specs/0032-palette-height-narrow.md)） | `palette-height` | 16 | 6.3秒 | 幅600/375での全8タブ・全15分類の高さ、件数で変わらないこと、公式タブでのtextareaの高さ、ページが縦にはみ出さないこと、縦横スクロールで最後の記号・タブ・分類に届くこと、幅1199/1440で変わらないこと（0064で幅1199pxの公式タブの上限を157px→180pxに上げた） |
| 統計・確率と集合・論理（[0064](specs/0064-palette-beyond-highschool.md)） | `palette-stats` | 20 | 18.3秒 | 演算子・関係子に足した記号の挿入、足した記号28件すべてが検索で引けること・1つの式にまとめてもエラーが出ないこと、公式の3分類が2段目に出ること、15件すべての挿入でエラーが出ないこと、「正規分布」「covariance」で引いて挿入できること、「転置」が引けないままであること（範囲外の記録）、幅1440pxの縦帯の高さ845pxと関係子タブでスクロールしないこと、幅1199pxで関係子28件がギリシャ小文字31件を超えないこと・演算子26件が136px以下・基本が94pxのまま・検索欄がタブと同じ行に残ること、幅360pxで0032の蓋のまま、2行以上並んだボタンが押せること |
| アプリの名前（[0065](specs/0065-app-name.md)） | `name` | 16 | 9.2秒 | タブのタイトル・ツールバーの見出しが `ThothGlyph` であること、幅1440pxでの見出しの幅（105.8px）、幅721px（名前が出る最小の幅）で横スクロールが出ないこと、幅720pxで1×1pxに隠れたままであること、幅600/360pxの横スクロール、manifestの名前、**旧キー `matheditor:` からの読み継ぎ**（文書・テーマ・言語・領域の幅と、写したあと旧キーが消えること、移行後の保存） |

全区分は238.9秒。区分ごとに初期状態へ戻すぶん、分ける前（20.8秒）より長い。
**選んで流せば短い**のが狙いで、`theme` 単独なら4.6秒。重いのは `file-load` で、
26.9 kBの文書を読み込むため。

### 落ちたときに何が残るか

[0027](specs/0027-verify-flaky.md) で入れた。

- **落ちたチェックは末尾に再掲する。** 区分名・ラベル・実測値つき。スクロールで
  流れても末尾だけ見れば分かる。全件OKならこの節は出ない。
- **結果は `tmp/verify-result.json` にも書く。** 出力を切ってしまっても後から読める
  （出力先は環境変数 `VERIFY_JSON` で変えられる）。
- **タイミングに依存するチェックには `⏱` が付く**（JSONでは `"timing": true`）。
  現在6件。「落ちてもよい」ではなく、**落ちたときに疑う順番**を示す印で、
  判定そのものは印の有無で変わらない。
- **時間で消える表示を見るチェックは、読む前に出るのを待つ**（`waitForFunction`）。
  コピーや読み込みの結果は数秒で消えるので、待たずに読むと空文字と比べて揺れる
  （0033で足したチェックが実際に揺れた）。
- **`--repeat N` で繰り返して揺れを見る。** 回ごとに結果が割れたチェックだけを
  目立たせる。1回でも落ちれば終了コードは1。

```bash
node scripts/verify-ui.mjs --repeat 5 perf
```

**出力を `tail` などで切り詰めない。** 件数だけが残っても、フレークと本物の
回帰を区別できない（実際に1度、42/43で落ちた実行の中身を取り逃がしている）。

### READMEのスクリーンショット（[0035](specs/0035-readme-for-users.md)）

`node scripts/make-screenshots.mjs` で `docs/screenshots/` の3枚
（1440×900のライトとダーク、360×667のスマートフォン）を撮り直す。
条件（localStorageを消した初期表示、サンプル文書、数式とパレットの描画待ち）を
スクリプトが揃えるので、手で撮らない。**画面の見た目を変えたissueでは撮り直す。**

### 色の測り方

配色は目で見て決めない。`getComputedStyle` から文字色と背景色を取り、
WCAGの相対輝度でコントラスト比を計算して4.5:1以上を確かめる
（スクリプトの `contrastOf`）。初期表示のちらつきは、`waitUntil: 'commit'` で
読み込み直後から背景色を繰り返し読み、意図しない色が現れないことで見る。

### 性能の測り方

400節・400数式の文書をスクリプトが生成し、20文字を連続入力して
「1文字あたりの入力反映」と「入力停止からプレビューが追いつくまで」を測る。
自動保存が動く状態のまま測る（保存のコストも体感に効くため）。

直近の実測（2026-09-21、`--repeat 5` の幅）: **35.8〜40.2ms/文字**（基準50ms以内）、
**282〜1497ms**（基準1.5秒以内）。実装前は約1200ms/文字だった。

**0010の前後で測り直した結果、1文字あたり約6ms増えている**
（前 31.6〜34.1ms → 後 35.8〜40.2ms）。`data-line` のぶんサニタイズと
DOMへの反映が増えるため。どちらも基準内だが、**追いつくまでの時間は
最悪値が1497msで基準の1.5秒に余裕がない**（[0059](issues/0059-perf-headroom.md)）。

> ここに前から書いてあった「29.2ms/文字・300〜500ms」は、測った環境が違う
> 古い値だった。**性能の数値を引き継ぐときは測り直す。**

### 読み込みの測り方（`scripts/measure-load.mjs`）

`npm run build` → `vite preview` で配る → ヘッドレスChromiumで2条件×3回開く、まで
スクリプトが自分でやる。測るのは **textareaが出るまで / プレビューに数式が出るまで /
パレットのラベルがKaTeXで描画されるまで**の3つで、中央値を取る。
成果物の大きさ（初期JS・遅延JS・初期CSS、gzip込み）と、遅延チャンクにKaTeX・marked・
DOMPurifyが入っていること（初期チャンクに入っていないこと）も同じスクリプトで見る。

スロットルの条件は Chrome DevTools の Fast 3G 相当（下り1.6Mbps・上り750kbps・
RTT 150ms）とCPU 4倍遅く、で固定する。**条件を変えると過去の数値と比べられない。**

直近の実測（[0024](specs/0024-bundle-size.md) の実装後）:

| | 初期JS | Fast 3G + CPU 4倍 textareaまで | 同 数式まで |
|---|---|---|---|
| 分割前 | 579.98 kB（gzip 180.50） | 1964 ms | 2021 ms |
| 分割後 | 243.20 kB（gzip 76.82） | 1085 ms | 1939 ms |

結果は `tmp/measure-load.json` にも書く。1件でも基準を外れたら終了コード1。

## 4. テストを足すときの指針

- **不具合を直すときは、まず失敗するテストを書く。** 直ってから書くと、
  そのテストが本当に不具合を捕まえるか分からない。
- **性能のissueは、起票の時点で現状を実測して数値を残す。** 「速くなった気がする」で
  終わらせない。
- 受け入れ基準は**外から観測できる結果**で書く。「保存しない」ではなく
  「保存状態の表示が出ない」。内部状態で書くと、実装を変えたときに基準ごと嘘になる。
- 落ちているテストを消して通すのは、直したことにならない。直せないなら
  `it.fails()` に変えて理由をissueに書く。

## 関連

- [要求仕様](requirements.md) — 非機能要求の数値の出どころ
- [アーキテクチャ](architecture.md) — どこが純粋関数で、どこがDOMに依存するか
- [機能仕様](functional-spec.md) — 実機検証が確かめている振る舞い
