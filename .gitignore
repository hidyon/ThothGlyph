# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
# dist-file は**わざと追跡している**（0074）。GitHubから落としてそのまま
# index.html を開けるようにするため。ソースを変えたら `npm run build:file` で
# 作り直してコミットすること（さもないと古い出力が配られる）。
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 検証スクリプトの出力
tmp/
